# 一番初めのpaneを選択
#select-pane -t 0
# 上下にウィンドウを分割する
#split-window -v
# 1番目(下側)のウィンドウを選択
#select-pane -t 1
# 左右にウィンドウを分割
#split-window -h
# メインとなるウィンドウの高さを35行に設定
#setw main-pane-height 35
# 上下分割レイアウトを反映
#select-layout main-horizontal
#display-panes
# 2番目(右下)のpaneの横幅（境界)を右に22文字幅移動する
# resize-pane -R -t 2 22


# select-pane -t 0
# split-window -h
# resize-pane -t 2 22
# select-pane -t 2
# split-window -v
# select-pane -t 1
# split-window -h
# resize-pane -L -t 0 22
# resize-pane -R -t 2 12
# select-pane -t 2


# echo "TMUX is ready"
#!/bin/sh
run 'vim -c :q'
run-shell "echo 'test echo 1'"
# vim -c :q
